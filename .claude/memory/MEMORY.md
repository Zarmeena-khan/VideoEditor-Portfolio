# Memory Index

This directory stores persistent memories about the user, project context, and learned preferences.

Each memory is a separate markdown file with frontmatter containing:
- `name`: kebab-case identifier
- `description`: one-line summary
- `metadata.type`: user | feedback | project | reference

Currently no memories stored. Memories will be added as the project evolves.
